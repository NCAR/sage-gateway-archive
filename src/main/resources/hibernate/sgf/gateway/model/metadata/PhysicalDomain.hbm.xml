<?xml version="1.0"?>

<!DOCTYPE hibernate-mapping PUBLIC
        "-//Hibernate/Hibernate Mapping DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">

<hibernate-mapping>

    <class name="sgf.gateway.model.metadata.PhysicalDomain" table="physical_domain" schema="metadata" lazy="false">

        <id name="identifier" column="id" type="sgf.gateway.utils.hibernate.UUIDUserType" access="field"/>

        <version name="version" column="version" unsaved-value="null" access="field"/>

        <property name="name" column="name" type="string" access="field"/>

    </class>

    <query name="findPhysicalDomainByName" cacheable="false">
        <![CDATA[
            from PhysicalDomain domain where upper(domain.name) = upper(:name)
        ]]>
    </query>

</hibernate-mapping>
