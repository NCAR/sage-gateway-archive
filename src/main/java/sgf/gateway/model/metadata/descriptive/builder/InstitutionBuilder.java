package sgf.gateway.model.metadata.descriptive.builder;

import sgf.gateway.model.metadata.descriptive.Institution;

public interface InstitutionBuilder {

    Institution build(String institutionName);
}
