package sgf.gateway.model.metadata.activities.modeling.builder;

import sgf.gateway.model.metadata.activities.modeling.Ensemble;

public interface EnsembleBuilder {

    Ensemble build(String name);

}
