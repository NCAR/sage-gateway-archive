<?xml version="1.0" encoding="UTF-8"?>

<web-app version="3.0"
         xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd">

    <!-- Web application name -->
    <display-name>ESG-CET Gateway</display-name>

    <!-- Root of Spring configuration files -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>classpath:sgf/gateway/application-context.xml</param-value>
    </context-param>

    <!-- Unhandled Exception Filter - traps any any unhandled exceptions from propagating to the UI -->
    <filter>
        <filter-name>unhandledExceptionFilter</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
        <init-param>
            <param-name>targetBeanName</param-name>
            <param-value>unhandledExceptionFilter</param-value>
        </init-param>
        <init-param>
            <param-name>targetFilterLifecycle</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>unhandledExceptionFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Hibernate Session Filter - mantains data access open throughout rendering of the view -->
    <filter>
        <filter-name>hibernateSessionFilter</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
        <init-param>
            <param-name>targetBeanName</param-name>
            <param-value>hibernateSessionFilter</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>hibernateSessionFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Spring Security delegating filter -->
    <filter>
        <filter-name>springSecurityFilterChain</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>springSecurityFilterChain</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter>
        <filter-name>hiddenHttpMethodFilter</filter-name>
        <filter-class>org.springframework.web.filter.HiddenHttpMethodFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>hiddenHttpMethodFilter</filter-name>
        <url-pattern>*.htm</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>hiddenHttpMethodFilter</filter-name>
        <url-pattern>*.html</url-pattern>
    </filter-mapping>

    <!-- Access Metrics Filter Chain  -->
    <filter>
        <filter-name>accessMetricsFilter</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
        <init-param>
            <param-name>targetBeanName</param-name>
            <param-value>accessMetricsFilter</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>accessMetricsFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Listener that loads the Spring web application context
        (from the specified value of contextConfigLocation) -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <!-- Listener that exposes the HttpSession mutex for locking purposes.
         May be used by Spring controllers to prevent multiple invocations by same user (double-clicking). -->
    <listener>
        <listener-class>org.springframework.web.util.HttpSessionMutexListener</listener-class>
    </listener>

    <!-- Listener for recording user click-streaming activity. -->
    <listener>
        <listener-class>sgf.gateway.web.listeners.ClickStreamHttpSessionListener</listener-class>
    </listener>

    <!-- Gateway application Spring Dispatcher servlet -->
    <servlet>
        <servlet-name>gateway</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <!-- All gateway servlet configuration, including Web MVC, is already referenced from application-context.xml -->
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value></param-value>
        </init-param>
        <load-on-startup>0</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>gateway</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <servlet>
        <servlet-name>openid</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <!-- All gateway servlet configuration, including Web MVC, is already referenced from application-context.xml -->
        <!-- <init-param> -->
        <!--     <param-name>contextConfigLocation</param-name> -->
        <!--     <param-value></param-value> -->
        <!-- </init-param> -->
        <load-on-startup>0</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>openid</servlet-name>
        <url-pattern>/myopenid/*</url-pattern>
    </servlet-mapping>

    <!-- View for web application root URL -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!--Redirection URL in case of server internal error (HTTP status code 500) -->
    <error-page>
        <error-code>500</error-code>
        <location>/common/error/logged-error.html</location>
    </error-page>

    <session-config>
        <session-timeout>90</session-timeout>
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

</web-app>
